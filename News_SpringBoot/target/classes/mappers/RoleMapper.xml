<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhouyi.mapper.RoleMapper">

    <!-- 根据ID查询角色 -->
    <select id="selectRoleById" resultType="com.zhouyi.entity.Role">
        SELECT id, role_name, description, status
        FROM roles
        WHERE id = #{id}
    </select>

    <!-- 根据角色名称查询角色 -->
    <select id="selectRoleByName" resultType="com.zhouyi.entity.Role">
        SELECT id, role_name, description, status
        FROM roles
        WHERE role_name = #{roleName}
    </select>

    <!-- 查询所有角色 -->
    <select id="selectAllRoles" resultType="com.zhouyi.entity.Role">
        SELECT id, role_name, description, status
        FROM roles
    </select>

    <!-- 插入角色 -->
    <insert id="insertRole" parameterType="com.zhouyi.entity.Role">
        INSERT INTO roles (role_name, description, status)
        VALUES (#{roleName}, #{description}, #{status})
    </insert>

    <!-- 更新角色 -->
    <update id="updateRoleById" parameterType="com.zhouyi.entity.Role">
        UPDATE roles
        SET role_name = #{roleName}, description = #{description}, status = #{status}
        WHERE id = #{id}
    </update>

    <!-- 删除角色 -->
    <delete id="deleteRoleById">
        DELETE FROM roles WHERE id = #{id}
    </delete>

</mapper>
